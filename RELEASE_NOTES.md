# VPS Tunnel Guardian 2.1.0

新增多隧道和 SOCKS5 模式、主窗口与托盘批量启停、独立退避重连、监听归属验证、端口冲突恢复、异步 SSH 错误日志、暗色 UI 和当前用户 DPAPI 密码存储；修复配置弹窗及 askpass 接口问题。

保留本地转发（-L）与 SSH → 上游服务的使用方式，不修改系统代理或 Clash 配置。

下载 `VPS-Tunnel-Guardian-v2.1.exe` 即可运行，附带空配置。更新时先退出旧版，再启动新版；切换会短暂断开旧版管理的隧道。EXE 未签名。

已执行语法、构建及本地功能回归，包括独立重连、批量启停、配置保存和密码组件；这不是长期网络稳定性保证。
